name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

# None of these jobs writes anything back to the repository, so the token is
# narrowed to reading it. Without this they would receive whatever the
# repository default grants, which is usually far more than a test run needs.
permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Full check
        run: make check

  shells:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install shells and roff
        run: sudo apt-get update && sudo apt-get install -y zsh fish groff-base

      - name: Completions parse
        run: |
          bash -n completions/plexdo.bash
          zsh  -n completions/_plexdo
          fish -n completions/plexdo.fish

      - name: Manual page is clean
        run: groff -man -Tutf8 -ww -z man/plexdo.1

  powershell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: PowerShell completion parses
        shell: pwsh
        run: |
          $errs = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
              "completions/plexdo.ps1", [ref]$null, [ref]$errs) | Out-Null
          if ($errs) { $errs | ForEach-Object { $_.Message }; exit 1 }
          Write-Host "no parse errors"
